number=int(input("ENTER THE NUMBER :"))
reverse_number=0
temp=number
while number!=0:
    digit=number%10
    reverse_number = reverse_number*10 + digit
    number//=10
print("IT IS A PALINDROME") if(reverse_number==temp) else print("IT IS NOT A PALINDROME")
